# https://github.com/hacdias/webdav

address: 0.0.0.0
port: 6065

prefix: /
debug: false
noSniff: false
behindProxy: true
directory: /storage
permissions: CRUD
rules: []
rulesBehavior: overwrite

log:
  format: console
  colors: true
  outputs:
  - stderr

users:
  - username: jonas
    password: webdav4jonas
    permissons: CRUD
